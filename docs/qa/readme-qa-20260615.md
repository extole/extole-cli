# extole-cli README QA — 2026-06-15

Branch: `qa/readme-and-publish`  
CLI version under test: **0.6.0**  
Test account: client `863039761` (token minted via super-user API key)

## Install methods

| Method | Result | Notes |
|--------|--------|-------|
| Binary (`install.sh`) | **PASS** | `EXTOLE_INSTALL=$HOME/.extole-bin sh install.sh` → `0.6.0` |
| From source (`npm install` + `npm link`) | **PASS** | Requires Node on `PATH` (documented); `~/.npm-global` prefix avoids nix store EACCES |
| Homebrew (`extole/tap/extole`) | **FAIL (stale)** | Formula was pinned to **0.4.0**; bumped to **0.6.0** in separate PR |
| npm (`npm install -g @extole/cli`) | **BLOCKED** | Package was 404 before QA; publish attempted — see npm section below |
| Windows binary | **N/A** | Not tested on macOS host |

## Read-only command QA

Automated runner: [`scripts/qa-readonly.sh`](../scripts/qa-readonly.sh)  
Raw log: [`.qa-run.log`](.qa-run.log)

| Command group | Result | Notes |
|---------------|--------|-------|
| ping, whoami, auth status/list | PASS | |
| person get (unknown email) | PASS | Graceful empty/not-found handling |
| share-links lookup | PASS on qa account | README example `chrisbackfillcw214` requires account with data + scope |
| programs, programs --all | PASS | |
| campaigns quality-rules/maxmind/reward-rules | PASS | Campaign `7627170621923807977` (global CONFIGURATION) |
| audiences list | PASS | No named audiences on test account |
| components list/get/types/download | PASS | Download to `/tmp/extole-qa-dl` |
| zones list/core | PASS | No embed zones on test account |
| events listen, stream | PASS | `--tail` + `--duration` |
| webhooks list/get/dispatches/dispatch-results | PASS | |
| notifications | PASS | |
| reports recommended/types/describe | PASS | |
| reports run --download | **Account-dependent** | `summary_per_program` returns 400 on test account (flows not defined); CLI exits **1** correctly |
| health | PASS | Read-only domain checks |
| chat | PASS | |
| api search, api path | PASS | |
| schema | PASS | |
| rewards, rewards state-summary | PASS | |
| reward-suppliers list/get | PASS | |

**Skipped (write / side effects):** events fire --live, create/set/delete/deploy, health provision-dkim, feedback.

**CLI bugs found:** none requiring code fixes in this pass.

## Gaps fixed in this branch

1. **License:** `package.json` + `LICENSE` aligned to MIT (README already said MIT).
2. **README:** Added `npm install -g @extole/cli` install method.
3. **publish.yml:** Added `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}` fallback for CI publish when OIDC trusted publishing is not configured.
4. **Homebrew:** Formula bump 0.4.0 → 0.6.0 (sha256 verified from release assets); new `update-homebrew-tap.yml` workflow for future releases.

## npm publish

Attempted:

```bash
npm publish --access public --provenance=false
```

**Failed:** `403 Forbidden — Two-factor authentication or granular access token with bypass 2fa enabled is required to publish packages.`

The token at `~/npmjs_token` authenticates as npm user `extole` (`npm whoami`) but lacks publish permission with 2FA bypass. **Action required:** create an npm automation token (or granular token with Publish + Bypass 2FA) and either:

- Re-run publish locally, or
- Add as `NPM_TOKEN` secret on `extole/extole-cli` and re-run the Publish workflow / cut v0.6.1

After publish succeeds, verify:

```bash
npm view @extole/cli version   # → 0.6.0
npm install -g @extole/cli
extole --version               # → 0.6.0
```

## Required GitHub secrets (manual)

| Secret | Repo | Purpose |
|--------|------|---------|
| `NPM_TOKEN` | extole/extole-cli | CI npm publish fallback |
| `HOMEBREW_TAP_TOKEN` | extole/extole-cli | Auto-bump homebrew-tap on release |

## Process retrospective

- *Implement the plan as specified* — QA install + read-only commands, license/README/publish/homebrew fixes, npm publish (blocked on token 2FA policy).
